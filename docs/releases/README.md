# Release Notes

Add one Markdown file per release tag before pushing the tag, for example `v0.1.0.md`.

The release workflow uses that file as the draft GitHub Release body and fails if it is missing or empty.
